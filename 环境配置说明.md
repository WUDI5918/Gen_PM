# 软件运行环境配置说明

## ✅ 已完成的配置

### 1. 系统环境检查
- ✅ Node.js: v22.20.0
- ✅ npm: 10.9.3
- ✅ 项目依赖已安装（136个包）

### 2. 项目文件
- ✅ `start_server.bat` - 双击启动本地服务器的批处理文件

## 📋 需要配置的环境变量

### API 密钥配置
项目使用了 Google Gemini AI，需要配置 API 密钥。

**配置步骤：**

1. 打开项目根目录下的 `.env.local` 文件
2. 确保文件包含以下内容：
   ```
   VITE_GEMINI_API_KEY=你的API密钥
   ```
3. 将 `你的API密钥` 替换为您的实际 Google Gemini API 密钥

**如何获取 API 密钥：**
- 访问 [Google AI Studio](https://aistudio.google.com/app/apikey)
- 登录您的 Google 账号
- 创建或复制您的 API 密钥

## 🚀 启动项目

### 方法 1：使用批处理文件（推荐）
双击 `start_server.bat` 文件

### 方法 2：使用命令行
```bash
npm run dev
```

## 📦 项目依赖说明

当前项目使用的主要依赖：
- **React 19.2.0** - UI 框架
- **Vite 6.2.0** - 构建工具
- **TypeScript 5.8.2** - 类型系统
- **@google/genai** - Google Gemini AI SDK
- **lucide-react** - 图标库
- **idb** - IndexedDB 封装库

## 🔧 常用命令

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm preview
```

## ⚠️ 注意事项

1. 确保 `.env.local` 文件中的 API 密钥已正确配置
2. 首次运行前必须先执行 `npm install` 安装依赖
3. 开发服务器默认运行在 `http://localhost:5173`
4. 不要将 `.env.local` 文件提交到版本控制系统

## 🐛 常见问题

### 问题：双击 bat 文件后窗口闪退
**解决方案：** 在命令行中手动运行 `npm run dev` 查看具体错误信息

### 问题：提示找不到模块
**解决方案：** 重新运行 `npm install`

### 问题：API 调用失败
**解决方案：** 检查 `.env.local` 中的 API 密钥是否正确

## 📞 技术支持

如遇到其他问题，请检查：
1. Node.js 版本是否符合要求（建议 v18 或更高）
2. 网络连接是否正常
3. 防火墙是否阻止了本地服务器端口
